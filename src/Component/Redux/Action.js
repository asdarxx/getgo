export const setEvoucherData = (payload) => {
    return{
        type:'SETDATA',
        payload,
    }
}

export const setTipe = (payload) => {
    return{
        type:'SETTIPE',
        payload,
    }
}